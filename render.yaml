build:
  pythonVersion: 3.11.8

services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python Kluch_s_bot.py
    plan: free
    autoDeploy: true